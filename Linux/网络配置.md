- 查看windows网络配置：ipconfig
- 查看Linux网络配置：ifconfig
- 重启网络服务：service network restart
- 固定IP
- 查看主机名：hostname （可以用 vim /etc/hostname来修改主机名）
- 设置host映射
	- Linux中在 /etc/hosts 文件指定即可，如 192.168.200.1 thank
- 查看DNS域名解析缓存：ipconfig /displaydns
- 手动清除dns缓存：ipconfig /flushdns
